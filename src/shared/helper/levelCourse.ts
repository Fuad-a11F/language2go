export const levelCourse = (lever: string) => {
  if (lever === "beginner") return "начинающего";
  else if (lever === "medium") return "среднего";
  else if (lever === "advanced") return "продвинутого";
};
